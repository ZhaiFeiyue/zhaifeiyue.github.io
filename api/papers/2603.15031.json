{
  "id": "2603.15031",
  "title": "Attention Residuals",
  "authors": [
    "Guangyu Chen",
    "Yu Zhang",
    "Jianlin Su",
    "Weixin Xu",
    "Siyuan Pan"
  ],
  "date": "2026-03",
  "source": "arXiv",
  "url": "https://arxiv.org/abs/2603.15031",
  "category": "llm",
  "secondary_tags": [
    "algorithm"
  ],
  "core_contribution": "把 PreNorm 的恒定权重残差累加替换为'softmax-attention 沿深度加权求和'：每层学一个伪 query w_ℓ 对所有先前层输出 v_i 做 softmax 选择性聚合 h_ℓ = Σ α_{i→ℓ} v_i。Block-AttnRes 训练 overhead < 4%, 已用于 Kimi Linear 48B-A3B (GPQA +7.5)。",
  "summary": "AttnRes 解决 PreNorm 的根本问题: 标准残差 h_ℓ = h_{ℓ-1} + f(h_{ℓ-1}) 用固定权重 1 累加, 导致 hidden 范数 O(L) 增长且早期层贡献被稀释。AttnRes 用 softmax 替代固定权重: α_{i→ℓ} = exp(w_ℓᵀ RMSNorm(k_i)) / Σ exp(...), 每层都能 input-dependent 选择从哪些早期层取信息 (时间-深度对偶)。Block-AttnRes 是工程化版本: 只在 block 边界做 (每 block_size//2 层一次), block 内仍标准 +; 实际放在 attn 子层之前和 MLP 子层之前各一次. 新参数仅 2L·d (相对 FFN 可忽略), 必须 zero-init 让起始退化为均匀平均. 两阶段在线 softmax 合并支持 cache + partial block. 关键消融: softmax > sigmoid, single-head depth-attn > multi-head, RMSNorm on V 必需, DenseFormer 固定标量无效. 训练动态: hidden norm bounded periodic (vs baseline 单调增长), 梯度 norm 更均匀. Scaling law: Block-AttnRes 节省 ~1.25x compute. 已被 Kimi Linear 48B-A3B 使用 (1.4T, Muon + WSD), GPQA +7.5, < 4% overhead.",
  "key_findings": [
    "核心方程 h_ℓ = Σ_i α_{i→ℓ} v_i, softmax 取代固定权重",
    "Block-AttnRes 注入点: 每 transformer 层 2 次 (attn-pre + MLP-pre), block 内仍 +",
    "新参数仅 2L·d (Linear(d,1) 伪 query + RMSNorm 每注入点)",
    "Pseudo-query MUST zero-init (否则训练不稳)",
    "Softmax 必胜: sigmoid 或 fixed-scalar (DenseFormer) 都无效",
    "Single-head depth-attention > multi-head (无收益增 params)",
    "Hidden norm: baseline 单调增长 vs AttnRes bounded periodic (block 边界 reset)",
    "Gradient norm 沿深度更均匀 → 早期层得到更强信号",
    "Scaling law: Block-AttnRes 节省 ~1.25x compute (matches baseline 在更少 compute)",
    "Kimi Linear 48B-A3B 实战: GPQA +7.5, training overhead < 4%, I/O ~5.5d/token/layer",
    "Block-size=6 sublayers 是 Kimi Linear 配置 (= 3 transformer 层一个 block)"
  ],
  "limitations": [
    "新参数虽少(每层仅一个d维向量)但增加了L^2级别的注意力计算",
    "Block AttnRes的block大小是超参数，需要调优",
    "Pipeline parallelism下仍有<4% overhead",
    "仅在MoE架构上验证，dense模型效果未知"
  ],
  "infra_impact": {
    "algorithm": "将time-depth duality形式化，用attention替代residual连接是新的研究方向",
    "kernel": "N/A — 计算为标准矩阵乘法",
    "framework": "需要framework支持cross-layer communication，pipeline parallelism需cache-based策略",
    "llm": "证明标准residual connection是LLM性能瓶颈之一，Block AttnRes是drop-in替换",
    "agent": "间接受益：模型质量提升"
  },
  "related_paper_ids": [
    "2506.08371",
    "2507.20534",
    "2510.26692",
    "2602.02276"
  ],
  "open_questions": [
    "AttnRes 在 100B+ dense 模型上的扩展性？目前仅 MoE Kimi Linear 验证",
    "Pseudo-query 维度 = d 是必需还是可压缩 (low-rank)？",
    "Block size 6 是否对其他 scale 也最优？",
    "Multi-head depth attention 真的没用还是消融 setup 不充分？",
    "AttnRes 与 dynamic depth (early exit) 的结合潜力？"
  ],
  "read_depth": "deep",
  "date_read": "2026-04-18"
}